<?xml version="1.0" encoding="UTF-8"?>
<!-- TEMPLATE ONLY. Create this Named Credential per org in Setup (or fill username/password and deploy).
     Basic auth to Jira Cloud: username = an Atlassian SERVICE-ACCOUNT email, password = its API token.
     NEVER commit a real token. -->
<NamedCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <label>Jira Egress</label>
    <endpoint>https://project-soho.atlassian.net</endpoint>
    <generateAuthorizationHeader>true</generateAuthorizationHeader>
    <principalType>NamedUser</principalType>
    <protocol>Password</protocol>
    <username>REPLACE_WITH_ATLASSIAN_SERVICE_ACCOUNT_EMAIL</username>
    <password>REPLACE_WITH_ATLASSIAN_API_TOKEN</password>
</NamedCredential>
