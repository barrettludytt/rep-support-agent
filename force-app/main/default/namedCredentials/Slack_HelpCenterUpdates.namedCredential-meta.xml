<?xml version="1.0" encoding="UTF-8"?>
<!-- TEMPLATE ONLY. Set <endpoint> to the Slack Incoming Webhook URL for #help-center-updates
     (Slack -> Apps -> Incoming Webhooks -> Add to #help-center-updates -> copy the generated URL).
     A webhook URL is a channel-scoped secret - NOT a user OAuth token - so no service user needed.
     NEVER commit the real URL. -->
<NamedCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <label>Slack Help Center Updates Webhook</label>
    <endpoint>https://REPLACE_WITH_SLACK_INCOMING_WEBHOOK_URL</endpoint>
    <generateAuthorizationHeader>false</generateAuthorizationHeader>
    <principalType>Anonymous</principalType>
    <protocol>NoAuthentication</protocol>
</NamedCredential>
